{
  "$schema": "https://railway.app/railway-schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "NODE_ENV=development npx --yes pnpm@9 install --no-frozen-lockfile && npx --yes prisma@5.22.0 generate --schema=./database/schema.prisma && npx pnpm@9 --filter api run build"
  },
  "deploy": {
    "startCommand": "node apps/api/dist/main.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
