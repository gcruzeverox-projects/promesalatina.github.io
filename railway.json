{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "NODE_ENV=development npx --yes pnpm@9 install --no-frozen-lockfile && $(find node_modules -name 'prisma.js' -path '*/prisma/bin/*' | head -1) generate --schema=database/schema.prisma && npx pnpm@9 --filter api build"
  },
  "deploy": {
    "startCommand": "node apps/api/dist/main.js",
    "restartPolicyType": "ON_FAILURE"
  }
}
